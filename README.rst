simp: sort Python imports simply
======================================

Runs through all the Python files, finds the first block of imports, sorts
them alphabetically, and leaves the rest of the file unchanged.
